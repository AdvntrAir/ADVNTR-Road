.env
.venv/
output/
outputs/
__pycache__/
*.pyc
.DS_Store

# Large binary assets — fonts and reference screenshots tracked separately
design_system/fonts/
graphical-inspiration/

# Root-level mount-rainier-np/ is a separate web project, not part of this pipeline
/mount-rainier-np/

# Intel Pack — Stage A drops the raw edition file here mid-run; incoming/ is
# scratch. The validated edition is committed straight into the Astro
# collection at apps/web/src/content/intel/, which is the archive now.
intel/incoming/
intel/out/

ADVNTR_Road_Build_Spec_FINAL.md
